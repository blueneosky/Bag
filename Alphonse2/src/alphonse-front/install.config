#!/usr/bin/env bash
typeset -A front_config # init array
# default values - adapt to your needs
front_config=(
    [bin_path]="/usr/local/lib/alphonse/alphonse-front"
    [webrsx_user]="www-data"
    [base_href]="/front"
)
